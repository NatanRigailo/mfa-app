apiVersion: v1
kind: Service
metadata:
  name: {{ include "mfa-app.fullname" . }}
  labels:
    {{- include "mfa-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mfa-app.selectorLabels" . | nindent 4 }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
